"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_router_dom_1 = require("react-router-dom");
function findMatchingRoutes(path, method, availableRoutes) {
    return availableRoutes
        .filter(function (route) { return route.method.toLowerCase() === method.toLowerCase(); })
        .map(function (route) {
        var match = react_router_dom_1.matchPath(path, { path: route.url, exact: true });
        if (match) {
            return {
                controllerName: route.controller.name,
                handler: route.handler,
                params: __assign({}, match.params),
            };
        }
    })
        .filter(function (route) { return route; });
}
exports.default = findMatchingRoutes;
