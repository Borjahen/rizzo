/// <reference types="express" />
import * as express from "express";
import { Container } from "inversify";
import { IControllerFactory } from "../interfaces";
import { IController } from "./controller";
export default class ControllerFactory implements IControllerFactory {
    container: Container;
    constructor(container: any);
    create(req: express.Request, res: express.Response, next: express.NextFunction, controller: string, handler: string): IController;
}
