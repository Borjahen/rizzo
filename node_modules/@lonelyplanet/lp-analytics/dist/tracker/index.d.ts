import { ITrackingProvider } from "../tracker/providers";
import { TTrackingEvent } from "../events";
export * from "./providers";
declare type TAtLeastOne<T> = {
    0: T;
} & T[];
export declare type TTrackingProviders = TAtLeastOne<ITrackingProvider>;
export declare const makeTracker: (analyticsPath?: string) => Tracker;
export declare class Tracker {
    private analyticsPath;
    private providers;
    constructor(providers: TTrackingProviders, analyticsPath?: string);
    initializeContainers(): void;
    track(event: TTrackingEvent): void;
    readonly snippet: string;
}
