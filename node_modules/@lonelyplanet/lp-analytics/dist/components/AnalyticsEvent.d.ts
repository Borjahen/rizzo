import * as React from "react";
import { TTrackingEvent } from "../events";
declare type renderMethod = ({ track }: IAnalyticsContextPayload) => React.ReactElement<any>;
declare type TTrackFunction = (event: TTrackingEvent) => void;
interface IAnalyticsContextPayload {
    track: TTrackFunction;
}
interface IAnalyticsEventProps {
    render: renderMethod;
}
declare class AnalyticsEvent extends React.Component {
    props: IAnalyticsEventProps;
    track: TTrackFunction;
    constructor(props: IAnalyticsEventProps);
    render(): React.ReactElement<any>;
}
export { AnalyticsEvent };
