import * as React from "react";
import container from "../config/container";
import TYPES from "../types";
export var ssr = function (_a) {
    var component = _a.component, props = _a.props;
    return (React.createElement("div", { dangerouslySetInnerHTML: { __html: render({
                component: component,
                props: props,
            }) } }));
};
export function render(options) {
    var core = container.get(TYPES.ICore);
    return core.render(options);
}
