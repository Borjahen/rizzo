import * as diacritics from "diacritics";
import _ from "./lodash";
export function replaceApostrophes(str) {
    return str.replace(/[ʻʼ’‘`´]/g, "'");
}
var removeDiacritics = diacritics.remove;
var cleanQuery = _.flow([
    replaceApostrophes,
    removeDiacritics,
    encodeURIComponent,
]);
export default cleanQuery;
