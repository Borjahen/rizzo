var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
import AuthRequestBuilder from "@lonelyplanet/open-planet-node/dist/authRequestBuilder";
import { authDefaults } from "../constants";
export var authSetupWithDefaults = function (params) {
    if (params === void 0) { params = {}; }
    var defaults = authDefaults;
    var options = params.options || {};
    var host = params.host || defaults.host;
    return AuthRequestBuilder.build({
        host: host,
        options: __assign({}, options, { clientId: options.clientId || defaults.options.clientId, scope: options.scope || defaults.options.scope, redirectUri: options.redirectUri || host.replace(/http:/, "https:") + "/sso", targetLinkUri: options.targetLinkUri || (typeof window !== "undefined" && window.location.origin + window.location.pathname) })
    });
};
export default {
    authSetupWithDefaults: authSetupWithDefaults,
};
