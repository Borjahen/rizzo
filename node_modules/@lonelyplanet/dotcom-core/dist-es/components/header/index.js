import * as React from "react";
import { ssr } from "../../classes/runtime";
export var Header = function () { return (React.createElement("header", null,
    ssr({ component: "globalautocomplete", props: { foo: "bar" } }),
    React.createElement("div", null, "logo"),
    ssr({ component: "globalnav", props: { foo: "baz" } }))); };
