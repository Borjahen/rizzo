import * as React from "react";
import { ITemplateProps } from "./interfaces";
export default class Template extends React.PureComponent<ITemplateProps, {}> {
    static Area: ({ children, className, }: ITemplateProps) => JSX.Element;
    static Position: ({ area, children, }: ITemplateProps) => JSX.Element;
    static Autocomplete: ({ children, className, }: ITemplateProps) => JSX.Element;
    static SearchActive: ({ children, className, }: ITemplateProps) => JSX.Element;
    render(): JSX.Element;
}
