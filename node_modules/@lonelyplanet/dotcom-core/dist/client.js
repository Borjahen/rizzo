Promise.resolve().then(function () {
  return require("./classes/runtime");
}).then(function (_a) {
  var render = _a.render;
  var els = document.querySelectorAll("[data-lpui-component]");
  Array.prototype.forEach.call(els, function (el) {
    var _a = el.dataset,
        component = _a.lpuiComponent,
        props = _a.lpuiComponentProps;
    render({
      component: component,
      el: el,
      props: JSON.parse(props)
    });
  });
});