#!/usr/bin/env node

const path = require("path");
const program = require("commander");

program
  .version("1.0.0")

program
  .command("build <component>", "build a component")
  .parse(process.argv);
