import { Close } from "backpack-ui/dist/components/icon";
import * as cn from "classnames";
import * as React from "react";
import * as styles from "./styles.css";

export interface ISearchInputCancelButtonProps {
  className?: string;
  onClick: (e: any) => void;
}

const SearchInputCancelButton = ({
  onClick,
  className,
}: ISearchInputCancelButtonProps) => (
  <button
    className={cn(styles.searchInputCancelButton, className)}
    onClick={onClick}
    type="reset"
  >
    {/* Use rizzo-next icon for consistency with global header */}
    <svg className="svg-icon" viewBox="0 0 32 32" aria-label="Close">
      <title>Close</title>
      <path d="M32 1.9l-1.9-1.9-14.1 14.1-14.1-14.1-1.9 1.9 14.1 14.1-14.1 14.1 1.9 1.9 14.1-14.1 14.1 14.1 1.9-1.9-14.1-14.1z" />
    </svg>
  </button>
);

export default SearchInputCancelButton;
