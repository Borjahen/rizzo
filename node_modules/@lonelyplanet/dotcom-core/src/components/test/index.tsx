import * as React from "react";

export default ({ foo }) => (
  <React.Fragment>
    <h1 className="foo">Hello {foo}</h1>
    <style>{`.foo { color: red };`}</style>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" />
    <script dangerouslySetInnerHTML={{ __html: "$(() => console.log(\"ready\"));" }} />
  </React.Fragment>
);
