import * as cn from "classnames";
import * as React from "react";
import { ITemplateProps } from "./interfaces";
import * as styles from "./styles.css";

const TemplateArea = ({
  children,
  className,
}: ITemplateProps) => (
  <div className={cn(styles.templateArea, className)}>
    {children}
  </div>
);

export default TemplateArea;
