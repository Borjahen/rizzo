"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var removeDecimalAndZeroFromPrice = function (price) { return price.replace(".0", ""); };
var addZeroToPrice = function (price) { return price + "0"; };
function fixFormattedPrice(price) {
    var priceSplit = price.split(".");
    if (priceSplit[1]) {
        if (priceSplit[1].length === 1 && priceSplit[1] === "0") {
            return removeDecimalAndZeroFromPrice(price);
        }
        if (priceSplit[1].length === 1) {
            return addZeroToPrice(price);
        }
    }
    return price;
}
exports.default = fixFormattedPrice;
