"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function hotelProvider(providers) {
    var booking = providers ? providers.filter(function (provider) {
        return provider.id.includes("bdc");
    }) : [];
    var hostelworld = providers ? providers.filter(function (provider) {
        return provider.id.includes("hw");
    }) : [];
    if (booking.length) {
        return "bdc";
    }
    else if (hostelworld.length) {
        return "hw";
    }
    return null;
}
exports.default = hotelProvider;
