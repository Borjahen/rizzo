import { IAuthRequestBuilder, IAuthRequestParams, IAuthRequestOptions } from "./interfaces";
export default class AuthRequestBuilder implements IAuthRequestBuilder {
    options: IAuthRequestOptions;
    authUri: string;
    static build(params: IAuthRequestParams): {
        builder: AuthRequestBuilder;
        googleLink: string;
        twitterLink: string;
        facebookLink: string;
        passwordLink: string;
        passwordlessLink: (email: any) => string;
    };
    constructor(params: IAuthRequestParams);
    buildUrl(queryObject: any): string;
    getLoginUrl(authMethod: any, redirect: any, settings?: {}): string;
    getRefreshUrl(authMethod: any, redirect: any, settings?: {}): string;
    getAccessTokenUrl(): string;
}
