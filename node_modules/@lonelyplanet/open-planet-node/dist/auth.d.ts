import { IAuth, IAuthParams, IGetAccessTokenParams, IGetAccessTokenResponse, IHttpClient } from "./interfaces";
export default class Auth implements IAuth {
    clientId: string;
    host: string;
    redirectUri: string;
    http: IHttpClient;
    constructor({clientId, host}: IAuthParams);
    private buildAccessTokenUrl(scope);
    getAccessToken(params: IGetAccessTokenParams): Promise<IGetAccessTokenResponse>;
}
