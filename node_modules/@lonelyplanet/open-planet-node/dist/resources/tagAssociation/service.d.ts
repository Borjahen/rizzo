import BaseService from "../../service";
import { IService, IResourceMapper, IFindParams } from "../../interfaces";
import TagAssociation, { ITagAssociation, ITagAssociationResource } from "./";
export interface ITagAssociationService extends IService<ITagAssociation> {
}
export default class TagAssociationService extends BaseService<ITagAssociation, ITagAssociationResource> implements ITagAssociationService {
    resource: string;
    mapper: IResourceMapper<ITagAssociation, ITagAssociationResource>;
    url(): string;
    findById(id: string, options?: IFindParams): Promise<TagAssociation<{}>>;
    find(options?: IFindParams): Promise<TagAssociation<{}>[]>;
    private serializeQuery(options);
}
