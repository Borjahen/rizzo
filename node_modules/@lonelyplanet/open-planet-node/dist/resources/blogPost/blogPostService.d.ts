import BaseService from "../../service";
import { IService, IResourceMapper } from "../../interfaces";
import { IBlogPost, IBlogPostResource } from "../blogPost";
export interface IBlogPostService extends IService<IBlogPost> {
}
export default class UserService extends BaseService<IBlogPost, IBlogPostResource> implements IBlogPostService {
    resource: string;
    mapper: IResourceMapper<IBlogPost, IBlogPostResource>;
    url(): string;
}
