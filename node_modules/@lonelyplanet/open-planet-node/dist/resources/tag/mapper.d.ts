import { IJsonApiResource } from "../../interfaces";
import Mapper from "../../mapper";
import { ITag, ITagResource } from "./";
import { ITagAssociationResource } from "../tagAssociation/index";
export declare class PoiMapper extends Mapper<ITag, ITagResource | ITagAssociationResource> {
    model: ITag;
    toModel(resource: ITagResource | ITagAssociationResource, included?: IJsonApiResource[]): ITag;
    private mapFromTagAssociation(model, resource);
    private mapFromTag(model, resource, included);
    toResource(model: ITag): ITagResource;
}
export default PoiMapper;
