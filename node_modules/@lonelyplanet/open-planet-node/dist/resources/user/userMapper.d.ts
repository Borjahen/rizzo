import { IUser, IUserResource } from "../user";
import { IJsonApiResource } from "../../interfaces";
import Mapper from "../../mapper";
export declare class UserMapper extends Mapper<IUser, IUserResource> {
    model: IUser;
    toModel(resource: IUserResource, included?: IJsonApiResource[]): IUser;
    toResource(model: IUser): IUserResource;
}
export default UserMapper;
