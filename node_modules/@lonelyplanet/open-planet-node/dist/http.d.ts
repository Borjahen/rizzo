import { IHttpClient, IFetchOptions } from "./interfaces";
export default class HttpClient implements IHttpClient {
    fetch<T>(url: any, options?: IFetchOptions): Promise<T>;
    private createError(url, options, response);
    private parseJsonApiError(textBody, statusCode);
}
